export type UserCredentials = {
    username: string,
    password: string,
    token?: string
}
export type UserInfo = {
    username?: string,
    fullName?: string,
    profilePictureURL?: string
}